import 'package:flutter/material.dart';

class DummyScaffold extends StatelessWidget {
  const DummyScaffold({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
